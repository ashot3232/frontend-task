export interface UserType {
  id: string;
  firstName: string;
  lastName: string;
  email: string;
  age: number;
}

export interface NoteType {
  id: string;
  title: string;
  description: string;
}
